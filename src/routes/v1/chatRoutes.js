const express = require("express");
// const chats = require("../../controller/chats.controller");
// const limiter = require("../../middleware/limiter");
const router = express.Router();

router.route("/");

module.exports = router;
